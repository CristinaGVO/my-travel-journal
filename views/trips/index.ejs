<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Journal Trips</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Your Journal Trips</h1>
    <a href="/users/<%=user._id%>/trips/new">Add an New Trip</a>
    <ul>
        <% trips.forEach((trip)=>{ %>
        <li><%= trip.destination %> at <%= trip.location %></li>
        <% }) %>
      </ul>
  </body>
</html>
